name: training_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: 13.0.2
  firebase_messaging: ^16.0.2
  flutter_local_notifications: ^18.0.1
  image_picker: ^1.2.0
  image: ^4.0.0
  flutter_image_compress: ^2.3.0
  file_picker: ^10.3.3
  url_launcher: ^6.3.2
  record: ^6.1.1
  audioplayers: ^6.5.1
  onesignal_flutter: ^5.3.4
  intl: ^0.20.2
  flutter_launcher_icons: ^0.14.4
  flutter_riverpod: ^2.5.1
  freezed_annotation: ^2.4.4
  http: ^1.2.1
  json_annotation: ^4.9.0
  logger: ^2.2.0
  logging: ^1.3.0
  path_provider: ^2.1.4
  path: ^1.9.0
  mime: ^1.0.5
  shared_preferences: ^2.2.3
  rxdart: ^0.28.0
  timeago: ^3.7.0
  flutter_markdown: ^0.7.4+1
  flutter_localizations:
    sdk: flutter
  patrol: ^3.19.0
  
  # Advanced Features
  # google_sign_in: ^6.3.0  # ❌ REMOVED - Using Email/Password only
  # sign_in_with_apple: ^7.0.0  # ❌ REMOVED
  crypto: ^3.0.6
  csv: ^6.0.0
  excel: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.9.0
  mocktail: ^1.0.3
  fake_cloud_firestore: 4.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/icon/icon.png
    - assets/privacy_policy_ar.md
    - assets/terms_of_service_ar.md
  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
        - asset: assets/fonts/Cairo-Bold.ttf
          weight: 700

# <<< --- تم نقل هذا القسم إلى هنا ---
# تأكد من أنه يبدأ من أول السطر
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"